<Window x:Class="dependencyProperties.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:dependencyProperties"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Button Height="50" Width="300" Content="Dale" FontSize="30" VerticalAlignment="Top" Background="DarkCyan">
            <Button.Style>
                <Style TargetType="Button">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="FontSize" Value="25"></Setter>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
        
        <Grid Margin="0 50 0 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <StackPanel Grid.Column="0" Grid.Row="0">
                <TextBox Width="100" Name="MiCuadro" Margin="60" Background="DarkSeaGreen" Text="{Binding ElementName=MiSlider, Path=Value, Mode=TwoWay, StringFormat={}{0:F2}}"></TextBox>
                <Slider Width="350" Name="MiSlider" Minimum="0" Maximum="100" Background="#FFC1F0C1"></Slider>
            </StackPanel>

            <StackPanel Grid.Column="1" Grid.Row="0">
                <Label Content="Nombre" ></Label>
                <TextBox Width="100" Height="30" Text="{Binding Path=Nombre, Mode=TwoWay}" Foreground="Beige" Background="Firebrick"></TextBox>
                <Label Content="Apellido" ></Label>
                <TextBox Width="100" Height="30" Foreground="Beige" Background="Firebrick" Text="{Binding Path=Apellido, Mode=TwoWay}"></TextBox>
                <Label Content="Nombre Completo" ></Label>
                <TextBox Width="100" Height="30" Foreground="Beige" Background="Firebrick" Text="{Binding Path=Nombre_completo, Mode=TwoWay}"></TextBox>
            </StackPanel>
        </Grid>
        
    </Grid>
</Window>
